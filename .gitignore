# Cordova-generated platform/plugin code (regenerated by `cordova platform add android`)
/platforms/
/plugins/

# Node
/node_modules/

# Build artifacts
*.apk
*.aab
.gradle/
local.properties

# Bundled fallback game, generated from docs/game.html by `npm run sync:game`
# (single source of truth is docs/game.html; this copy is packaged into the APK).
/www/game.html

# OS cruft
.DS_Store
